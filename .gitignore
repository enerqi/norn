# Build artifacts (Odin output tree, like Cargo's target/)
target/

# Compiled executables
*.exe

# Spall profiler trace (emitted when built with -define:SPALL_ENABLE=true)
trace.spall

# agents
.agents
.claude
.codegraph

# parity harness scratch outputs (the harness itself now lives in the consumer project)
parity_*.txt
probe_*.txt

# personal scratch notes (not shipped product)
PARITY.md
HANDOFF.md
.sublime/
